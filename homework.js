"use strict";
function multiplyNumber(first, second) {
    return first * second;
}
class Fulaninho {
    constructor(nome) {
        this.nome = nome;
    }
    dizSalve(dizSalve) {
        return (dizSalve + `Salveee ${this.nome}!`);
    }
}
