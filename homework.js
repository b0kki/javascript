"use strict";
function multiplyNumber(first, second) {
    return first * second;
}
class Fulaninho {
    constructor(nome) {
        this.nome = nome;
    }
    dizSalve(oi) {
        return (oi + `Salveee ${this.nome}!`);
    }
}
