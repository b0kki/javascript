const darkTheme = {
  pColor: '#fff',
  secondaryColor: '#eee',
  bgColor: '#282a35',
  bgButton: '#fff',
  borderColor: '#c1c1c1'
}

export default darkTheme

export type Theme = {
  pColor: string
  secondaryColor: string
  bgColor: string
  bgButton: string
  borderColor: string
}
